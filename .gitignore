# The local content checkout, the site build.sh produces, its per-version
# scratch directories, and the tools it downloads.
/modelplane/
/public/
/.build-*/
/node_modules/
/bin/
/htmltest
# Hugo's build artifacts and htmltest's cache.
/hugo_stats.json
/.hugo_build.lock
/resources/
/tmp/
